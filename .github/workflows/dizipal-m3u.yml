name: Dizipal M3U Oluştur

on:
  workflow_dispatch:  # Sadece manuel çalıştır
  schedule:
    - cron: '0 6 * * *'  # Her gün TSİ 09:00'da

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Repoyu İndir
        uses: actions/checkout@v4

      - name: Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Kütüphaneleri Yükle
        run: pip install cloudscraper requests beautifulsoup4

      - name: M3U Dosyasını Oluştur
        run: |
          cd /home/runner/work/_temp/
          python /home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/dizipal_m3u.py

      - name: M3U'yu Depoya Taşı
        run: |
          cp /home/runner/work/_temp/dizipal.m3u /home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/
          cd /home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/
          ls -la

      - name: Değişiklikleri Kaydet
        run: |
          cd /home/runner/work/${{ github.event.repository.name }}/${{ github.event.repository.name }}/
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add dizipal.m3u
          git commit -m "🤖 dizipal.m3u güncellendi" || echo "Değişiklik yok"
          git push
